class Solution:
    def multiply(self, num1: str, num2: str) -> str:
        str_num1 = int(num1)
        str_num2 = int(num2)

        prod = str_num1 * str_num2
        prod_str = str(prod)

        return prod_str
