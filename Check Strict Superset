super_set = set(map(int, input().split()))
n = int(input())

is_superset = True

for _ in range(n):
    subset = set(map(int, input().split()))
    if not super_set.issuperset(subset):
        is_superset = False
        break

print(is_superset)
