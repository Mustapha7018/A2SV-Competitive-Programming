class Solution:
    def findDisappearedNumbers(self, nums: List[int]) -> List[int]:

        output = []
        sett = set(nums)
        N = len(nums)

        for i in range(1, N+1):
            if i not in sett:
                output.append(i)
        
        return output


        
